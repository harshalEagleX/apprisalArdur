# Core
pymupdf==1.24.5
pyyaml>=6.0.1
pydantic>=2.7.0
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-dotenv>=1.0.1

# Durable job queue (Scaling Phase 1 — readme/SCALABILITY_PLAN.md).
# Wires the POST /qc/submit + GET /qc/job/{id} contract the Java backend already calls.
celery>=5.3.0
redis>=5.0.0

# Testing
pytest>=8.2.0
