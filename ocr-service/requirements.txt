# Python 3.11 compatible requirements for OCR Service
# Install with: pip install -r requirements.txt
# Then run: python -m spacy download en_core_web_md

# Web Framework
fastapi>=0.100.0,<0.115.0
uvicorn>=0.23.0,<0.31.0
python-multipart>=0.0.6

# PDF Processing
pymupdf>=1.23.0,<1.25.0
Pillow>=10.0.0,<11.0.0
pdf2image>=1.16.3

# Image Processing
opencv-python>=4.8.0

# Data Validation
pydantic>=2.0.0,<3.0.0

# OCR
pytesseract>=0.3.10

# HTTP Client (also used for ollama API calls)
httpx>=0.24.0,<0.28.0

# NLP - optional spaCy
# spacy>=3.7.0,<3.8.0   # uncomment if using spaCy rules
numpy>=1.24.0,<2.0.0

# Database
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
alembic>=1.12.0

# Job queue
celery>=5.3.0
redis>=5.0.0

# ML training pipeline (offline model fallback)
scikit-learn>=1.3.0
joblib>=1.3.0
pandas>=2.0.0

# System Dependencies (install manually):
# macOS: brew install poppler tesseract
# Ubuntu/Debian: sudo apt-get install poppler-utils tesseract-ocr

# LLM Backend (install separately — NOT via pip):
# 1. Install ollama:  https://ollama.com/download  (macOS: brew install ollama)
# 2. Pull model:      ollama pull llama3:8b-instruct-q4_0
# 3. Start server:    ollama serve
# The service auto-detects ollama at http://localhost:11434
