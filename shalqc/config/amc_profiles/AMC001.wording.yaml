# AMC001's verbatim "Reject as:" checklist text — SHALqc.md §6.3. NOT
# CONSUMED by any code in this build pass; app/report/wording.py raises
# NotImplementedError. Port from ocr-service/config/qc_rejection_catalog.yaml
# when Part 6/8 are built (that YAML is already close to this shape).

meta:
  amc_code: AMC001
  version: prof-AMC001-1.0.0

templates: {}
  # S-1.address_mismatch: "Property address does not match with order form."
