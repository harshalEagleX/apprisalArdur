"""app.routing (crt-1.0.0) — SHALqc.md §7 confidence routing between extraction
and rules. Public surface: the `router` singleton + `route()`."""
from app.routing.router import route, router  # noqa: F401
