import ReviewerLayout from "@/components/shared/ReviewerLayout";

export default function Layout({ children }: { children: React.ReactNode }) {
  return <ReviewerLayout>{children}</ReviewerLayout>;
}
